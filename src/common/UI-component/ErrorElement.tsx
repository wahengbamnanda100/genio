import { Box, Typography } from "@mui/material";

const ErrorElement = () => {
	return (
		<Box>
			<Typography variant="h4" color="error">
				Error
			</Typography>
		</Box>
	);
};

export default ErrorElement;
